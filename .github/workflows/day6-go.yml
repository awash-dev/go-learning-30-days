name: Go Day 6

on:
  push:
    paths:
      - 'day6/**'
      - '.github/workflows/day6-go.yml'
  pull_request:
    paths:
      - 'day6/**'
      - '.github/workflows/day6-go.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.22'

      - name: Build
        working-directory: ./day6
        run: go build -v ./...

      - name: Run
        working-directory: ./day6
        run: go run .
